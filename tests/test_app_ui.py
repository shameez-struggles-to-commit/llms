"""
This repository intentionally avoids bundling Python-based Playwright tests.

Use the existing Playwright tool in your MCP environment to run UI/E2E tests
against a running server instance (started via launch.ps1 or `llms --serve`).
"""
